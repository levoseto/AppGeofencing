<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="AppGeofencing.MainPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml">
    <StackLayout>
        <Frame
            Padding="24"
            BackgroundColor="Green"
            CornerRadius="0">
            <Label
                FontSize="36"
                HorizontalTextAlignment="Center"
                Text="Ejemplo de localización en 2do plano"
                TextColor="White" />
        </Frame>
        <StackLayout x:Name="slContenedor">
            <Button Clicked="Button_Clicked" Text="Start/Stop" />
            <ScrollView>
                <Label x:Name="locationLabel" />
            </ScrollView>
        </StackLayout>
        <StackLayout x:Name="slCarga" IsVisible="False">
            <ActivityIndicator x:Name="aiCarga" Color="Green" />
            <Label x:Name="locationLabel2" />
        </StackLayout>
    </StackLayout>
</ContentPage>